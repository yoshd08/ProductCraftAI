
'use client';

// This component is no longer used and can be considered removed.
// Keeping it empty to avoid breaking imports if it were referenced somewhere unexpectedly.
export default function AuthForm() {
    return null;
}
